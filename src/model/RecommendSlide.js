export default class RecommendSlide {
  constructor ({picUrl, url, targetId, targetType}) {
    this.picUrl = picUrl
    this.url = url
    this.targetId = targetId
    this.targetType = targetType
  }
}
